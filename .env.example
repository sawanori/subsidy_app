# Root Project Environment Variables Example
# This file serves as a template for environment configuration

# Development Mode
NODE_ENV=development

# Backend API
API_URL=http://localhost:3001
API_PREFIX=

# Frontend
FRONTEND_URL=http://localhost:3000

# Database (use backend/.env.local for actual credentials)
# DATABASE_URL=postgresql://user:password@host:port/database

# For monorepo coordination only
# Actual secrets should be in backend/.env.local and frontend/.env.local